<% if(title) { %>
    <h1><%= title %></h1>
<% } else { 
    res.redirect('/')
} %>

<ul class="notice" id="messages">
    
</ul>

<input type="hidden" name="account_id" <% if(locals.loggedin) {%> value="<%= locals.accountData.account_id %>" <% } %>>

<%- grid %>


<script src="../../js/inventory-item-details.js"></script>